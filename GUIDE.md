# 游戏渠道排行榜工具 — 使用说明书

版本 v2.0 — 2026-06-02

---

## 获取项目文件夹

向工具维护人（吴品达）索取压缩包 `game_rank_tool.zip`，解压到本地任意目录，例如：

```
D:\game_rank_tool\
```

解压后文件夹内应包含 `main.py`、`dashboard.py`、`config.yaml` 等文件。后续所有命令均在该文件夹内执行。

---

## 为什么要做这个

研究竞品时，团队日常面对几个反复出现的问题：某款游戏在 TapTap、B站、App Store 同时排多少名？下周有哪些竞品要开测？这款游戏近一个月排名是涨是跌？

过去的回答方式效率极低：分别打开各平台网站手动查询，数据散落各处，没有汇总；历史排名靠人工截图记录，无法快速对比趋势；开测信息散在平台公告里，靠人力追踪容易遗漏。

这个工具的目标是把这些信息自动化采集、集中展示，让产品、策划、市场同学每天打开一个页面就能掌握市场动态，不再重复做搬运工作。

---

## 这个系统是什么

多渠道游戏排行榜自动抓取 + 可视化看板，包含三个页签：

**国内排行榜**
- 覆盖 7 个平台：App Store、TapTap、B站、小米、快爆、WeGame、应用宝
- 支持下载榜、畅销榜、活跃榜、预约榜，每平台抓 Top 20
- 多平台横向对比表，自动标记日排名变化（▲▼ 数字 / NEW / —）
- 今日动态卡片：自动分析最大涨幅 / 最大跌幅 / 多平台同时上榜，并附趋势注释（如"连续 3 日上涨""双平台同步"）
- 渠道筛选器：勾选需要展示的平台，表格列顺序随之变化
- 指定游戏的历史排名趋势折线图

**国内开测表**
- 覆盖 B站、TapTap 近期焦点、好游快爆即将上线、WeGame 四个渠道
- 以时间为行、渠道为列的透视表，只显示今天及之后的开测信息
- 类型标色：绿色 = 首发/公测/不删档，蓝色 = 预约，橙色 = 删档测试

**海外排行榜**
- 移动平台：RuStore（俄区）、App Store（美/日/韩/英）、Google Play（全球及美/日/韩/英）
- PC 平台：Steam（美/日/韩/英）、Epic Games Store、Microsoft Store（美/日/韩/英）
- 支持下载榜、畅销榜、活跃榜（活跃榜仅 Steam 有）
- 同样包含今日动态卡片和渠道筛选器

数据每日通过 Windows Task Scheduler 在 09:00 自动抓取，存入本地 SQLite 数据库。

---

## 工作流程

```
首次配置
  └─ pip install -r requirements.txt（仅需一次）

        ↓

每日抓取（自动 / 手动）
  └─ python main.py fetch
       ├─ 国内排行榜：7 个平台 × 多榜单，约 1~2 分钟
       ├─ 国内开测表：B站 / TapTap / 快爆 / WeGame，约 30 秒
       └─ 海外排行榜：17 个平台，约 3~5 分钟（含 Playwright 渲染）

        ↓

启动看板
  └─ streamlit run dashboard.py
       └─ 浏览器访问 http://localhost:8501

        ↓

查阅数据
  ├─ 国内排行榜：切换日期 / 榜单类型，查看多平台对比表和趋势图
  ├─ 国内开测表：查看近期各渠道开测游戏
  └─ 海外排行榜：切换榜单类型，分别查看移动端和 PC 端排名
```

---

## 相关文件

以下文件均位于项目文件夹根目录下，拿到文件夹后无需移动，直接在该目录执行命令即可。

| 文件 / 目录 | 说明 | 是否需要手动修改 |
|-------------|------|-----------------|
| `main.py` | CLI 入口，所有命令从这里运行 | 否 |
| `dashboard.py` | Streamlit 看板，浏览器可视化 | 否 |
| `config.yaml` | 配置文件（平台开关、抓取数量、定时时间） | 按需调整 |
| `scrapers/` | 各平台爬虫（7 国内排行 + 4 开测表 + 17 海外排行） | 否 |
| `storage/db.py` | SQLite 数据库读写封装 | 否 |
| `data/rankings.db` | 数据库文件，首次 fetch 后自动生成 | 否 |
| `requirements.txt` | Python 依赖列表 | 否 |
| `feishu_export.py` | 将 GUIDE.md 导出为飞书文档的工具脚本 | 否 |

---

## 常用命令速查

```bash
# 抓取所有平台数据（国内排行 + 开测表 + 海外排行）
python main.py fetch

# 只抓排行榜，跳过开测表
python main.py fetch --no-launches

# 只抓指定平台
python main.py fetch --platform taptap

# 连通性测试（每个平台抓 Top 3，快速验证）
python main.py test

# 启动看板
streamlit run dashboard.py

# 导出当日数据到 Excel
python main.py export
```

---

## 看板功能说明

### 国内排行榜

左侧边栏选择日期和榜单类型（下载榜 / 畅销榜 / 活跃榜 / 预约榜），默认展示预约榜。

表格上方的渠道筛选器可勾选 / 取消某个平台，表格列顺序随勾选顺序变化。

今日动态卡片（有前日数据时自动出现）：

- 最大涨幅：该榜单当日排名上升最多的前三款游戏，附平台名
- 最大跌幅：排名下降最多的前三款游戏，附平台名
- 趋势注释：若某游戏连续多日同向变化或多平台同步变化，会在括号内标注，例如"连续 3 日上涨""双平台同步"
- 多平台上榜：同时在 3 个及以上平台出现的游戏

排名变化图例：▲ 绿色数字 = 排名上升，▼ 红色数字 = 排名下降，NEW = 新上榜，— = 未变化。

趋势图：在页面下方选择游戏和平台后，显示该游戏在该平台的历史排名折线图。

### 国内开测表

以开测时间为行、渠道为列的透视表，只显示今天及之后的日期，"待定"排在末尾。每格显示该渠道在该日的开测游戏及类型徽标。

颜色说明：绿色 = 首发 / 公测 / 不删档，蓝色 = 预约，橙色 = 删档测试，灰色 = 其他。

渠道筛选器同样支持勾选展示哪几个渠道。

### 海外排行榜

分为移动平台和 PC 平台两个区块，各自有独立的渠道筛选器。

侧边栏选择榜单类型：下载榜（所有平台）、畅销榜（App Store / Google Play / Steam）、活跃榜（Steam）。

同样包含今日动态卡片，逻辑与国内排行榜一致。

---

## 给 Claude Code 的操作指南

> 如果你是 Claude Code，请阅读本节并按步骤完整执行，目标是让看板在浏览器中成功运行。

### 前置确认

首先确认当前工作目录是项目文件夹根目录（即包含 `main.py`、`dashboard.py`、`config.yaml` 的那个文件夹）。如果不在该目录，请先 `cd` 进去再执行后续命令。

然后检查 Python 版本：

```bash
python --version
```

需要 Python 3.8 或以上版本。如果命令不存在，提示用户先安装 Python。

### Step 1：安装依赖

```bash
pip install -r requirements.txt
```

安装完成后继续，如有报错先排查依赖冲突。海外排行榜部分平台需要 Playwright，安装完成后还需执行：

```bash
playwright install chromium
```

### Step 2：抓取数据

```bash
python main.py fetch
```

- 预计耗时 3~7 分钟（含海外平台 Playwright 渲染）
- 终端会打印各平台抓取摘要表格，TOP1 列不为空说明成功
- 部分平台偶发超时属正常，只要多数平台有数据即可继续

### Step 3：启动看板

```bash
streamlit run dashboard.py
```

- 启动后浏览器自动打开 `http://localhost:8501`
- 左侧默认选中「预约榜」，「国内排行榜」页签应显示多平台对比表
- 切换到「国内开测表」页签，应显示近期开测游戏的时间轴
- 切换到「海外排行榜」页签，应显示移动端和 PC 端排名表格

### 验证完成的标志

- 国内排行榜：能看到带游戏名称的多平台对比表，部分游戏旁有 ▲▼ 变化标记
- 国内开测表：能看到按日期排列的 B站 / TapTap / 快爆 / WeGame 四列表格
- 海外排行榜：能看到移动平台和 PC 平台的排名表格

### 常见问题处理

| 现象 | 原因 | 解决方法 |
|------|------|----------|
| 「数据库暂无数据」 | 未运行 fetch | 先执行 Step 2 |
| 端口 8501 被占用 | 有其他 streamlit 在运行 | 加参数 `--server.port 8502` |
| 某平台数据全为空 | 该平台接口当天异常 | 不影响其他平台，可忽略或稍后重试 `python main.py fetch --platform taptap` |
| `ModuleNotFoundError` | 依赖未安装完整 | 重新执行 Step 1 |
| 海外排行榜全为空 | Playwright 未安装 | 执行 `playwright install chromium` |
| 开测表为空 | 当天无今日及之后的开测数据 | 属正常，等待次日 fetch |
